[project]
name = "semiconductor-defect-vision"
version = "0.1.0"
description = "Computer vision and ML system for semiconductor SEM defect analysis"
requires-python = ">=3.10,<3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
